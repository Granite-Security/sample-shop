services:
  accounting-postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=accountingdb'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5439:5432'
