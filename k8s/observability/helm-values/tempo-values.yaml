# Single-binary Tempo (this chart's only mode) — local filesystem trace
# storage backend is the default and is fine at this project's scale.
tempo:
  retention: 72h

persistence:
  enabled: true
  storageClassName: local-path
  size: 10Gi

serviceMonitor:
  enabled: true
